tcp:
  routers:
    gost-router:
      rule: "HostSNI(`google.com`)"
      entryPoints:
        - https-entry
      service: mtg-service
      tls:
        passthrough: true

  services:
    mtg-service:
      loadBalancer:
        servers:
          - address: "mtg:3128"

